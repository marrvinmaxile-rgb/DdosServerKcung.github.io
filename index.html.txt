<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VOID•NET | SAMP ASSAULT v4.2</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=VT323&family=Press+Start+2P&display=swap');
        
        body {
            margin: 0;
            padding: 0;
            background: linear-gradient(180deg, #0a0a0a, #1a0000);
            color: #00ff41;
            font-family: 'VT323', monospace;
            height: 100vh;
            overflow: hidden;
        }
        
        .scanline {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: linear-gradient(transparent, rgba(0,255,65,0.03), transparent);
            background-size: 100% 300%;
            animation: scan 6s linear infinite;
            pointer-events: none;
            z-index: 9999;
        }
        
        @keyframes scan { 0% {background-position:0 0;} 100% {background-position:0 -300%;} }
        
        header { 
            background: rgba(10,10,10,0.95); 
            padding: 15px 25px; 
            border-bottom: 3px solid #00ff41; 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
        }
        .logo { font-family: 'Press Start 2P', cursive; font-size: 26px; text-shadow: 0 0 15px #00ff41; }
        
        .container { display: flex; height: calc(100vh - 75px); }
        .sidebar { width: 300px; background: #111; padding: 20px; border-right: 2px solid #00ff41; overflow-y: auto; }
        .main { flex: 1; padding: 20px; display: flex; flex-direction: column; gap: 20px; }
        
        .panel { background: #1a1a1a; border: 1px solid #00ff41; padding: 18px; box-shadow: 0 0 20px rgba(0,255,65,0.3); margin-bottom: 15px; }
        input { width: 100%; padding: 12px; margin: 8px 0; background: #000; border: 1px solid #00ff41; color: #00ff41; font-size: 18px; }
        button { width: 100%; padding: 14px; margin: 8px 0; background: #000; color: #00ff41; border: 2px solid #00ff41; font-size: 18px; cursor: pointer; }
        button:hover { background: #00ff41; color: black; }
        .attack-btn { background: #ff0033; border-color: #ff3366; font-size: 26px; padding: 20px; font-weight: bold; }
        
        .terminal { 
            background: #000; 
            height: 340px; 
            padding: 15px; 
            overflow-y: auto; 
            border: 1px solid #00ff41; 
            font-size: 17px;
        }
    </style>
</head>
<body>
    <div class="scanline"></div>
    
    <header>
        <div class="logo">VOID<span style="color:#ff0033;">NET</span> — SAMP CRASHER</div>
    </header>
    
    <div class="container">
        <div class="sidebar">
            <div class="panel">
                <h2>TARGET SERVER</h2>
                <input type="text" id="target" value="45.79.XXX.XXX:7777" placeholder="IP:PORT">
                <button onclick="startAttack()" class="attack-btn">🚀 LAUNCH DDOS ATTACK</button>
                <button onclick="stopAttack()">⛔ STOP ATTACK</button>
            </div>
        </div>
        
        <div class="main">
            <div class="panel">
                <div class="terminal" id="terminal">
                    <div>> VOID•NET C2 Server Online • 1427 bots ready</div>
                </div>
            </div>
        </div>
    </div>

    <script>
        function addLog(text) {
            const term = document.getElementById('terminal');
            const line = document.createElement('div');
            line.innerHTML = `> <span style="color:#00ff88;">${text}</span>`;
            term.appendChild(line);
            term.scrollTop = term.scrollHeight;
        }
        
        function startAttack() {
            const target = document.getElementById('target').value;
            addLog(`ATTACK STARTED ON ${target}`);
            addLog("Sending massive UDP flood to SAMP server...");
            addLog("Server is lagging hard...");
            addLog("Players are disconnecting...");
        }
        
        function stopAttack() {
            addLog("ATTACK STOPPED. All traces cleaned.");
        }
    </script>
</body>
</html>
